<slot></slot>

<style global>
  body {
    margin: 0;
    overflow: hidden;
  }

  * {
    box-sizing: border-box;
  }

  button, label, audio, select, input[type="file"] {
    cursor: inherit;
  }
</style>